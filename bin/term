#!/bin/bash

ID=$(xdpyinfo | grep focus | grep -oE '0x[0-9a-fA-F]+')
PID=$(ps --ppid $(xprop -id $ID | grep -m 1 PID | cut -d " " -f 3) -o pid=)
if [ -L "/proc/${PID# }/cwd" ]; then
    alacritty --working-directory $(readlink /proc/${PID# }/cwd)
else
    alacritty
fi
